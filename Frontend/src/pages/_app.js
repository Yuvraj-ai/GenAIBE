import '../styles/globals.css';
import 'katex/dist/katex.min.css'; // Add this line for KaTeX styles

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;